# AGENTS — our-lady-of-lourdes-church

> Port of the Church of Our Lady of Lourdes, Singapore — 50 Ophir Road, Singapore 188690. First Tamil Catholic church, blessed 13 May 1888 (cornerstone 1 Aug 1886, Fr Joachim Meneuvrier MEP 1884) — Neo-Gothic modelled on the Lourdes basilica, lancet windows and stained glass of the 15 rosary mysteries, Lourdes grotto with Our Lady and St Bernadette. Two bombs in 1942, territorial parish 1974, National Monument No. 52 (gazetted 14 Jan 2005, restored 2009–2010). *A grotto of welcome in the city.* Static SPA — no backend, no DB, no SSR. For deep conventions, workflow, and design system detail, read `CLAUDE.md`.

## Stack

`React 19.2.8` + `Vite 7.3.6` + `Tailwind CSS 4.3.3` (`@tailwindcss/vite 4.1.17`, CSS-first `@theme` inline in `src/index.css`) + `TypeScript 5.9.3` strict + `React Router 7.18.2` `HashRouter` + `vite-plugin-singlefile 2.3.3` (primary `dist/index.html` + `dist/images/` for GH Pages / S3) + `eslint 9.39.5` flat + `vitest 3.2.6` (`jsdom 26.1.0`) + `@testing-library/react 16.2.0` + `playwright 1.55.1` (chromium) · alias `@` → `src/` (sync `vite.config.ts` `path.resolve(__dirname,"src")` ↔ `tsconfig.json` `paths: {"@/*":["src/*"]}` + `baseUrl:"."`) · `pnpm 11.0.0` (`packageManager` + `engines node>=20`, `pnpm-lock.yaml` committed, `--frozen-lockfile` in CI), `npm` works · all deps pinned exact — no `^` in `package.json` (re-pin on upgrade, update docs)

## Commands

All commands verified in `package.json` `scripts`. Don't document a script until it exists there.

| Command | Purpose |
|---|---|
| `pnpm install` | Install deps (Node 20+ for Vite 7, pnpm 11) — pnpm is the supported path; `npm ci` needs `--legacy-peer-deps` (typescript-eslint 8.28.0 peer range predates TS 5.9) |
| `pnpm dev` | Vite HMR dev server (default `http://localhost:5173`) |
| `pnpm build` | Production single-file build (`vite build && node scripts/inject-csp-hashes.mjs`) → `dist/index.html` (script-src sha256-pinned) + `dist/images/` |
| `pnpm preview` | Preview `dist` locally |
| `pnpm typecheck` | Type gate `tsc --noEmit` — **run before every push** |
| `pnpm lint` | ESLint flat (`eslint . --max-warnings 0`) |
| `pnpm lint:fix` | ESLint auto-fix (`eslint . --fix`) |
| `pnpm test` | Vitest `jsdom` `run` — harness configured (`vitest 3.2.6` + `jsdom 26.1.0`) but **no `*.test.*` files in current worktree** (`src/test/` absent; `vite.config.ts` still references `setupFiles: ["src/test/setup.ts"]` — add a setup file before re-adding tests) |
| `pnpm test:watch` | Vitest watch mode (`vitest`) |
| `pnpm test:coverage` | Vitest with coverage (`vitest run --coverage` via @vitest/coverage-v8) |
| `pnpm test:e2e` | Playwright `chromium` (10 specs + `helpers.ts` in `e2e/`: `smoke`, `navigation`, `mobile-navigation`, `ministries`, `give-faq`, `enhancements`, `enhancements-round5`, `enhancements-round7`, `enhancements-round19`, `deep-links`) |
| `pnpm test:e2e:built` | Playwright vs built artifact (`playwright.built.config.ts` → `vite preview :4173` or `E2E_BASE_URL` for live host) |
| `pnpm test:e2e:ui` | Playwright UI mode |
| `pnpm test:e2e:report` | Open last Playwright HTML report |
| `pnpm lint && pnpm typecheck && pnpm test && pnpm test:e2e && pnpm build` | Pre-push gate — lint + typecheck + build are authoritative; test/e2e reflect current harness state (no unit tests present) |

## Structure

```
src/ (41 source files; no test harness in current worktree)
  App.tsx              # HashRouter + 17 Route path entries (16 content paths + * NotFound) inside the Layout wrapper Route (18 <Route> tags total; see Routing below)
  main.tsx             # StrictMode + createRoot + resolveHashRedirect pre-mount rewrite (stripped deepLinks — no JSDoc)
  index.css            # @theme tokens (33 colors incl. gold-700 #85641c + terracotta-600 #8f5038 + pine accents + 2 shadows = 35) + @layer base/utilities (utilities: text-balance, bg-adobe-texture, bg-gold-bloom, bg-grain, divider-weave, divider-weave-thin, gold-rule, gold-rule-left, hero-ken-burns, img-zoom, mask-fade-b, reveal, reveal-visible, rise-in + rise-in-d1..d4, menu-in, drawer-in, drawer-item-in, page-in, dot-pulse, card-lift, card-tint, link-underline, skip-link + scrim-hero, scrim-page, hero-fade, rule-draw + 9 keyframes hero-ken-burns/rise-in/menu-in/drawer-in/drawer-item-in/page-in/halo-pulse/hero-fade/rule-draw + bloom-drift/img-zoom + themed scrollbar (sapphire thumb on parchment track, webkit + scrollbar-color) + @media print reveal override)
  components/          # Layout (+SkipLink +BackToTop +ScrollProgress; anchor effect returns its cleanup), Header (useScrolled(16) + aria-label="Primary" desktop nav + aria-current nav states + menu-in/drawer-in entrances + modal drawer rendered OUTSIDE <header> (a backdrop-filter on the header would otherwise become the fixed drawer's containing block and collapse it to the header height): dialog/aria-modal/focus-trap/focus-restore + Escape handler incl. window-level Escape for the desktop dropdown + stateful "Open menu"/"Close menu" hamburger + outside-click guard that ignores the toggle + drawer closes only on link taps + Give CTA in the drawer + drawer nav aria-label="Mobile"), Footer (nav landmarks aria-label="Explore" / "Get involved" + Tamil name + SocialIcons + Archdiocese of Singapore text link + address/phone/email), PageHero (rise-in staged content, fetchPriority="high" on hero image), Emblem, Timeline (dot-pulse halos + scroll-drawn rail), SocialIcons (Facebook custom SVG — Instagram/YouTube absent in site.ts), SafeImage (local fallback), BackToTop (threshold 480, aria-label="Back to top", progress ring, reduced-motion-aware), ui/{Button (active press),Container,SectionHeading,Accordion (animated grid-rows collapse + inert closed panels),Reveal}
  hooks/               # useScrolled.ts (threshold 12 default; Header passes 16) + useScrollProgress.ts (0..1, rAF-throttled) + useScrollSpy.ts (document-order tie-break, drives the Ministries pill `aria-current`) — 3 hooks
  pages/               # Home, About, History, Worship, Ministries, NewsEvents, Serve, Give, FAQ, NotFound (10 pages, all named exports: Home, About, History, Worship, Ministries, NewsEvents, Serve, Give, FAQ, NotFound)
  data/                # nav.ts (primaryNav[6] + footerNav[10] with description on children), content.ts (TimelineEntry/GroundsPlace/Ministry/FaqItem/EventItem/GivingOption/Priest/PpcMember + priests[3] (Rev Fr Alphonsus Dominic — Parish Priest from 22 Jul 2026 / Rev Fr Leo Justin HGN — Assistant, Heralds of Good News, reappointed 1 Nov 2024 / Fr Joachim A. M. Meneuvrier MEP — Founding Missionary 1884) / ppcMembers[6] (Parish Priest ex-officio + Chairperson/Vice Chairperson/Secretary/Treasurer/Member) / lifeTimeline[7 1884–2005] (1884 A Shepherd for the Indians Meneuvrier → 1885 Land at the Rochor Bend → 1886 Cornerstone Laid 1 Aug → 1888 A Basilica in Miniature 13 May, first Tamil church → 1942 War at Ophir Road → 1974 A Parish for All territorial → 2005 National Monument No. 52) / grounds[3] (main-church/grotto/garden, each with image+imageFallback+imageAlt — all local) / ministries[6] (liturgical/faith-formation/pastoral-care/family-life/youth/community) / faqs[7] (Mass times/confession/how to get there/parking/dress & visitor guidelines/baptism-marriage/join ministry) / upcomingEvents[6] (title+date+summary+category Parish|Devotion|Formation|Archdiocese — Parish Feast 11 Feb Our Lady of Lourdes + Rosary/Divine Mercy etc.) / givingOptions[6] (PayNow, Cheque payable Church of Our Lady of Lourdes, Cash at Office, Mass Offerings, St Vincent de Paul) / serveRoles[4] (Liturgical/Catechists/Pastoral Care/Hospitality) / devotions[6] (Holy Rosary Mon Wed Thu Fri 11:35, Divine Mercy Tue 11:35, Saturday Rosary 16:15, Reconciliation 15 min before Mass, Parish Feast 11 Feb, Grotto Prayer daily) / visitorGuidelines {dress[5], other[6]} + images {hero/heroFallback/chapel→grotto/sanctuary/garden/hall/feast — all local — 7 keys}), site.ts (site as const: name Church of Our Lady of Lourdes / shortName Our Lady of Lourdes / tamilName தூய லூர்து அன்னை ஆலயம் / tagline A grotto of welcome in the city. / vision To be a welcoming Marian household, drawing all to Christ through worship, formation, and service — in English, Tamil, and every tongue that finds a home at Ophir Road. + address {street/city/zip/full+query getters} 50 Ophir Road Singapore 188690 + hours {church open for Mass, gates close 17:00 public holidays / office Mon–Fri 09:00–17:00 closed holidays / reception Mon–Fri 09:00–17:00 / adoration Before weekday Mass Rosary/Divine Mercy 11:35 / confessionWeekday 15 min before each Mass / confessionWeekend 15 min before each Mass} + mass {weekdayNoon Mon–Fri 12:30 English / weekdayEvening Mon–Fri 19:00 Tamil / saturday 17:00 + 18:15 + 19:30 English / sunday[5] 08:00 English / 09:30 Tamil / 11:00 English / 12:30 English / 18:30 Tamil + publicHoliday 09:00 English · 10:00 Tamil + confession 15 min before Mass + adoration Rosary Mon/Wed/Thu/Fri 11:35 Divine Mercy Tue 11:35 Sat Rosary 16:15 + note public holidays office closed gates 17:00} + contact {officePhone +65 6294 0624 / fax +65 6294 2686 / email colol.secretariat@catholic.org.sg} + transport {mrt Bugis EW12/DT14 · Rochor DT13 · Jalan Besar DT22 / buses Ophir/Victoria: 2,7,12,32,33,51,61,63,80,130,133,145,197} + feast {Our Lady of Lourdes · 11 February} + uen "" (no UEN) / chequePayee Church of Our Lady of Lourdes / facebook ChurchOfOurLadyOfLourdes / instagram "" / youtube "" / archdiocese catholic.sg / mapsUrl + mapsEmbedSrc 50 Ophir Road + url/ogImage ourladyoflourdes.sg)
  utils/               # cn.ts — twMerge(clsx), always merge via cn() + massDay.ts — massDayKey(date): 'weekdays'|'saturday'|'sunday', single source for the Worship today-highlight (getDay switch) + monogram.ts — monogram(name): "Fr Alphonsus Dominic" → "AD", honorific stripping + deepLinks.ts — knownRoutePaths + resolveHashRedirect (stripped of JSDoc) + categoryTone.ts — categoryTone(category) for EventMeta chips
  components/EventMeta.tsx # shared categoryTone + EventMeta chip (Home + NewsEvents)
vite.config.ts         # alias @→src + test { globals, jsdom, setupFiles: src/test/setup.ts (file absent in current worktree; add before re-adding tests), include: src/**/*.{test,spec}.{ts,tsx}, exclude: e2e/** } + server.watch.ignored [skills/**, dist/**, playwright-report/**, test-results/**, coverage/**, src.orig/**] + viteSingleFile()
tsconfig.json          # strict + noUnusedLocals/noUnusedParameters/noFallthroughCasesInSwitch/isolatedModules/noEmit + include [src, vite.config.ts, eslint.config.js, playwright.config.ts, playwright.built.config.ts] + types [node, vitest/globals] + paths @/*
eslint.config.js       # flat config (typescript-eslint 8 + react-hooks 5 + react-refresh); ignores [dist, node_modules, coverage, playwright-report, test-results, skills, src.orig]
playwright.config.ts   # Playwright 1.55.1 (chromium, webServer → pnpm exec vite :5173, expect timeout 15s; CSP is a meta tag in index.html, post-build sha256-pinned by scripts/inject-csp-hashes.mjs)
playwright.built.config.ts # extends playwright.config.ts but serves dist/ via vite preview :4173 (or E2E_BASE_URL for live host)
e2e/                   # 10 spec files + helpers.ts (smoke, navigation, mobile-navigation, ministries, give-faq, enhancements, enhancements-round5, enhancements-round7, enhancements-round19, deep-links + helpers)
.github/workflows/ci.yml # CI: lint → typecheck → test → test:e2e → build + artifacts (Node 24, pnpm 11) — mirrors the green gate (triggers `branches: [main]` on push + pull_request)
public/                 # 10 images (hero-church.jpg, grotto.jpg, sanctuary.jpg, garden.jpg, community.jpg, liturgical.jpg, pastoral-care.jpg, faith-formation.jpg, family-life.jpg, youth.jpg → dist/images/) + `_headers` (nosniff/DENY/strict-origin + Permissions-Policy + HSTS) + `favicon.svg` + `robots.txt`
index.html             # title Church of Our Lady of Lourdes — Singapore; Google Fonts Fraunces + Source Sans 3; CSP meta `script-src 'self' 'unsafe-inline'` in source (rewritten to sha256 hashes by scripts/inject-csp-hashes.mjs after build), `style-src 'unsafe-inline'` kept for React inline styles, `img-src 'self' data: blob:`, `object-src 'none'`, `frame-src https://www.google.com` (maps embed); OG tags + Church JSON-LD for Church of Our Lady of Lourdes (ourladyoflourdes.sg) — og:image ourladyoflourdes.sg/images/hero-church.jpg, image alt Neo-Gothic facade on Ophir Road
scripts/               # inject-csp-hashes.mjs — post-build: hashes inline <script> bodies and rewrites CSP script-src to sha256-… list
```

## Quirks — would break if guessed wrong

- **HashRouter is intentional** — static hosts (GH Pages / S3) have no SPA fallback. Don't switch to `BrowserRouter` without adding a `404.html` redirect. Deep links are `/#/worship`, `/#/ministries#liturgical`, etc.
- **`viteSingleFile()` inlines JS+CSS** — `public/images/` is still copied to `dist/images/` (Vite `publicDir` is not inlined; upload both). No assumed code-splitting. Dynamic `import()` that expects chunks will be inlined or break.
- **`scripts/inject-csp-hashes.mjs` hardens CSP** — source `index.html` keeps `script-src 'unsafe-inline'` for Vite dev preamble; `pnpm build` hashes every inline `<script>` in `dist/index.html` and rewrites the meta CSP to `sha256-…` list. Don't re-add `unsafe-inline` in the built artifact; style-src keeps `unsafe-inline` for React inline styles (`ScrollProgress` width etc.).
- **Alias `@` must stay in sync** — `vite.config.ts` (`path.resolve(__dirname,"src")`) ↔ `tsconfig.json` (`paths: {"@/*":["src/*"]}`, `baseUrl:"."`) — change both.
- **Tailwind v4 has no `tailwind.config.js`** — tokens live only in `src/index.css` `@theme`. Don't add arbitrary `bg-[#...]`; extend `@theme` with a named `bsc-*` token. Tokens are 33 colors (incl. `gold-700 #85641c` + `terracotta-600 #8f5038` + `pine` accents) + 2 shadows = 35. If a `bsc-*` class is referenced but has no token, the UI silently falls back — keep `@theme` and class names in sync.
- **TS strict will fail on unused code** — `noUnusedLocals:true` + `noUnusedParameters:true` + `noFallthroughCasesInSwitch:true` + `isolatedModules:true` + `noEmit:true`. Clean unused vars/params before commit.
- **Test/lint harness** — `eslint 9.39.5` flat + `vitest 3.2.6` (jsdom) + `@testing-library/react 16.2.0` + `playwright 1.55.1` (chromium, `channel: "chromium"` new headless — rAF-throttled scroll hooks need BeginFrames) — gate is `lint && typecheck && test && test:e2e && build`. Current worktree has **no `src/test/` harness and no `*.test.*` files** (vitest configured but `pnpm test` reports no tests); `vite.config.ts` `test.setupFiles` still points at `src/test/setup.ts` — create it before re-adding unit tests. `eslint` ignores `skills/` + `src.orig/`; `vite.config.ts` `test` + `server.watch.ignored` + `tsconfig.json` `types [vitest/globals]` are required.
- **`skills` is vendored, git-tracked reference content — PRESENT on disk and in git (catalog + per-skill SKILL.md + scripts) — tooling ignores it: `eslint.config.js` `ignores` + tsconfig `include` scoping + `vite.config.ts` watch-ignores. Don't lint, type-check, or import from it. The catalog lives at `skills/skills-catalog.md`.
- **Google Fonts loaded in `index.html`** — `Fraunces` (display) + `Source Sans 3` (body). CSP in that file whitelists `fonts.googleapis.com`/`fonts.gstatic.com` + `google.com` for the maps iframe. No `upload.wikimedia.org`/`images.pexels.com` allowlist (all `images.*` are local). Don't add runtime font loaders in components.
- **`Layout.tsx` handles hash scroll** — double-hash aware (`#/ministries#liturgical` → split on `#` + strip `/`) + `setTimeout 80ms` + fallback `window.scrollTo`. Current anchor targets are `#mass`/`#confession`/`#visit` on `/worship` and `#liturgical`/`#faith-formation`/`#pastoral-care`/`#family-life`/`#youth`/`#community` on `/ministries`. Preserve when extending layout. Layout also wraps outlet in a keyed `page-in` container (`data-testid="page-container"` + `data-route`) so route changes replay entrance while hash-only updates keep the node.
- **`vite.config.ts` `server.watch.ignored`** — ignores `**/skills/**`, `**/dist/**`, `**/playwright-report/**`, `**/test-results/**`, `**/coverage/**`, `**/src.orig/**` to avoid `ENOSPC` file-watcher limit from the vendored `skills/` tree (contains large `.venv` when present) and archived `src.orig/`.
- **`SafeImage` fallback** — `src/components/SafeImage.tsx` wraps `<img>` with `fallback` default `/images/hero-church.jpg`, `loading="lazy"` default, optional `fetchPriority` (set `"high"` on above-the-fold heroes — Home hero + PageHero), and `onError` → `dataset.fallback` guard to swap `src` once. All current `images.*` are local. Use `SafeImage` for any future external image; don't use bare `<img>` for CDN sources.
- **SkipLink never rewrites the hash** — `src/components/SkipLink.tsx` `preventDefault`s and imperatively focuses `#main-content` (`<main>` in `Layout`). A native jump would rewrite the hash and route to NotFound under HashRouter.
- **Header close-on-activation** — the mobile drawer closes on any in-drawer link activation (`onClickCapture` on the drawer `<nav>`), not only on pathname change: a tap on a link to the **current** route never changes `pathname`, so the `useEffect([pathname,hash])` close cannot fire. Desktop dropdown `<ul>` also closes on child-link click. Drawer top-level links carry `aria-current="page"` when any child is active; hamburger is `h-11 w-11` (44px). Header also handles `Escape` to close menus/drawer.
- **Header modal drawer** — the mobile drawer is a modal dialog (`role="dialog"` + `aria-modal="true"` + `aria-label="Site menu"` + `tabIndex={-1}`): the panel receives initial focus on open, `Tab`/`Shift+Tab` cycle inside it (trap in `handleDrawerKeyDown`), every close path (toggle, `Escape`, in-drawer link, route change, outside `pointerdown`) restores focus to the hamburger toggle (`drawerWasOpenRef` guard keeps mount from stealing focus), and a `pointerdown` outside the drawer closes it. Don't downgrade it back to a disclosure or remove the trap.
- **`useScrolled` threshold** — `src/hooks/useScrolled.ts` defaults to `12`; `Header.tsx` calls `useScrolled(16)` to delay the transparent→solid switch on Home. Don't "fix" the mismatch — it's intentional.
- **`ScrollProgress` is decoupled from Header** — `src/components/ScrollProgress.tsx` is a fixed `h-[3px]` rail (`data-testid="scroll-progress"`, `aria-hidden`, `scaleX(progress)`) rendered by `Layout` at `z-[60]`; it is not inside `Header`. `Header` solid calculation now includes `mobileOpen`.
- **`Ministries` jump nav** — uses `<Link to="/ministries#id">` (not plain `<a href="#id">`) to preserve HashRouter route; plain `#id` would replace the hash and route to NotFound. Renders `ministries.map` → 6 pill links (`#liturgical`, `#faith-formation`, `#pastoral-care`, `#family-life`, `#youth`, `#community`) with `aria-label="Jump to ministry"` + `aria-current="true"` on the pill matching the `useScrollSpy` active section (document-order tie-break; falls back to the first id) + alternating `bg-bsc-cream`/`bg-bsc-parchment` sections.
- **"Sacred Motion" package** (all transform/opacity only, gated by the global `prefers-reduced-motion` block in `index.css`):
  - **Staged entrances** — `.rise-in` + `.rise-in-d1..d4` delay steps animate Home hero and PageHero content on mount (keyframe `rise-in`, fill `both` → settles at opacity 1; instant under reduced motion).
  - **Menu entrances** — `.menu-in` on the desktop dropdown `<ul>`, `.drawer-in` on the mobile drawer (both conditionally rendered, so the keyframe runs on mount).
  - **Accordion collapse** — panels use the `grid-template-rows 0fr→1fr` technique (`grid grid-rows-[0fr|1fr]` + inner `overflow-hidden`), NOT `hidden`. Closed panels carry `aria-hidden="true"` + `inert` so screen readers/keyboard skip them; `aria-expanded` on the button is the single source of truth. Arrow-key navigation cycles items (ArrowUp/Down/Home/End).
  - **Card hover system** — `.card-lift` (lift + `shadow-bsc` + gold border tint) is the one true card hover; grounds/devotion/pillar/role/giving/event cards all use it. `.card-tint` is the info-card variant (border + bg tint on hover). Don't re-add ad-hoc `hover:-translate-y-*` chains.
  - **Link affordances** — `.link-underline` draws a gold underline via a `::after` scaleX transform (footer nav, top-bar Give link). Buttons add `active:translate-y-0 active:scale-[0.98]` press feedback.
  - **Timeline halo** — `.dot-pulse::after` is an expanding gold ring (keyframe `halo-pulse`, infinite); reduced-motion sets it to `opacity: 0`.
- **`BackToTop` contract** — `src/components/BackToTop.tsx` (mounted in `Layout` before `<Footer/>`): appears when `window.scrollY > 480`, hides below (`aria-hidden` + `tabIndex -1` + `pointer-events-none` when hidden), click → `window.scrollTo({ top: 0, behavior: matchMedia('(prefers-reduced-motion: reduce)') ? 'auto' : 'smooth' })`. It never touches the hash (HashRouter-safe). It carries a progress ring (`data-testid="back-to-top-progress"` + inner `circle[data-progress]`) whose `stroke-dashoffset` fills with `useScrollProgress`. When it hides while focused, it blurs itself first (focus must not rest inside an `aria-hidden` subtree).
- **Worship "today" Mass highlight** — `src/utils/massDay.ts` `massDayKey(date)` maps `getDay()` 0→sunday / 6→saturday / rest→weekdays via a `switch`; `Worship.tsx` renders the matching card via a local `MassCard` with `data-testid="mass-card"` + `data-card-day` + `data-today="true"` + gold top rule + "Today" chip (exactly one per calendar day — never hardcode a day or duplicate the mapping).
- **Round-5 package** — event cards render the category inside a bordered chip (`rounded-full` + `categoryTone` color) with the date in `font-display` beside it (shared `src/components/EventMeta.tsx` — `categoryTone` + `EventMeta`); the History story column is `lg:sticky lg:top-28 lg:self-start` (`data-testid="history-story"`) beside the timeline; the Timeline rail is `[data-testid="timeline-rail"]` (`bg-gradient-to-b from-bsc-sapphire-400 via-bsc-gold-400/70 to-bsc-gold-500`) — drawn on scroll via IntersectionObserver (`scale-y-0` → `scale-y-100`); `.img-zoom` drifts only inside `.group` cards; `.bg-gold-bloom` decorates the Home + Give dark bands; Button icons render inside an `aria-hidden` span with `group-hover:translate-x-0.5` nudge; NotFound carries the ghost `Emblem` + `rise-in` staging; About pillars use ghost `font-display text-5xl` numerals and priest cards carry `aria-hidden` monogram discs (`src/utils/monogram.ts` `monogram()` — strips `fr/friar/rev/msgr/mep/ofm/hgn` etc.).
- **`aria-current` nav contract** — Header plain links get `aria-current="page"` when `pathname === item.to`; dropdown parent buttons get `aria-current="true"` + gold tint when any `child.to.split("#")[0] === pathname`; dropdown child links get `aria-current="page"` on exact `pathname + hash === child.to`; Ministries pills get `aria-current="true"` on the scrollspy active section (`useScrollSpy`, document-order tie-break). Mobile drawer parents get `aria-current="page"` when a child route is active (e.g. `/history` → About parent current). Header hamburger is `h-11 w-11` (44px touch target) — don't shrink it.

## Conventions

- **Routing:** `App.tsx` is the only route table — 17 `Route` path entries (16 content paths + `*` NotFound) inside the Layout wrapper (18 `<Route>` tags) covering 10 pages, with 7 alias paths in 5 groups (`/worship` canonical for `/mass-times`+`/hours-location`+`/visit`; `/ministries` canonical for `/ministry`; `/news-events` canonical for `/news-and-events`; `/serve` canonical for `/volunteer`; `/give` canonical for `/donate`) and hash anchors on two pages: `/worship` → `#mass`/`#confession`/`#visit` (Mass schedule, Confession & Adoration, Find Us) and `/ministries` → `#liturgical`/`#faith-formation`/`#pastoral-care`/`#family-life`/`#youth`/`#community` (6 ministry sections; sixth id in data is `community`, titled Community & Outreach). Nav is driven by `src/data/nav.ts` (`primaryNav[6]` with `description` on children + `footerNav: NavLink[10]`) — update there, `Header`/`Footer` render from it. `Header` dropdowns and `Ministries` jump nav must use `<Link to="/path#id">`, never `<a href="#id">`.
- **Data:** `src/data/content.ts` is the data layer — 8 exported interfaces (`TimelineEntry`, `GroundsPlace`, `Ministry`, `FaqItem`, `EventItem`, `GivingOption`, `Priest`, `PpcMember`) and 10+ exports: `priests[3]` (Rev Fr Alphonsus Dominic — Parish Priest from 22 Jul 2026 / Rev Fr Leo Justin HGN — Assistant, Heralds of Good News, reappointed 1 Nov 2024 / Fr Joachim A. M. Meneuvrier MEP — Founding Missionary 1884) + `ppcMembers[6]` (Parish Priest ex-officio + Chairperson / Vice Chairperson / Secretary / Treasurer / Member) + `lifeTimeline[7]` spanning **1884–2005** Ophir Road (1884 A Shepherd for the Indians → 1885 Land at the Rochor Bend → 1886 Cornerstone Laid 1 Aug → 1888 A Basilica in Miniature 13 May, first Tamil church → 1942 War at Ophir Road → 1974 A Parish for All territorial → 2005 National Monument No. 52, restored 2009–2010) + `grounds[3]` (`main-church`/`grotto`/`garden`, each with `image`+`imageFallback`+`imageAlt` — all local) + `ministries[6]` (`liturgical`/`faith-formation`/`pastoral-care`/`family-life`/`youth`/`community` → Community) + `faqs[7]` (Mass times, confession 15 min before each Mass, how to get there Bugis/Rochor/Jalan Besar, parking compound first-come, dress & visitor guidelines, baptism/marriage via secretariat, join a ministry) + `upcomingEvents[6]` (`title`+`date`+`summary`+`category` Parish|Devotion|Formation|Archdiocese — Parish Feast Our Lady of Lourdes 11 Feb Triduum + Novena/Rosary/Divine Mercy etc.) + `givingOptions[6]` (PayNow, Cheque payable Church of Our Lady of Lourdes, Cash at Office, Mass Offerings, St Vincent de Paul — icons globe/church/book/heart/flame/sprout) + `serveRoles[4]` (Liturgical/Catechists/Pastoral Care/Hospitality) / `devotions[6]` (Holy Rosary Mon Wed Thu Fri 11:35, Divine Mercy Tue 11:35, Saturday Rosary 16:15, Reconciliation 15 min before Mass, Parish Feast 11 Feb, Grotto Prayer daily) / `visitorGuidelines` {dress[5], other[6]} + `images` (7 keys: `hero`/`heroFallback`/`chapel→grotto`/`sanctuary`/`garden`/`hall→community`/`feast→grotto` — all local). `src/data/site.ts` (`site as const`) is the canonical single source for `name` Church of Our Lady of Lourdes / `shortName` Our Lady of Lourdes / `tamilName` தூய லூர்து அன்னை ஆலயம் / `tagline` A grotto of welcome in the city. / `vision` To be a welcoming Marian household, drawing all to Christ through worship, formation, and service — in English, Tamil, and every tongue that finds a home at Ophir Road. + `address` 50 Ophir Road Singapore 188690 (`full`/`query` getters) + `hours` (6 keys: `church` open for Mass, gates 17:00 holidays, `office` Mon–Fri 09:00–17:00 closed holidays, `reception` Mon–Fri 09:00–17:00, `adoration` Before weekday Mass 11:35, `confessionWeekday` 15 min before each Mass, `confessionWeekend` 15 min before each Mass) + `mass` (weekdayNoon 12:30 English, `weekdayEvening` 19:00 Tamil, `saturday` 17:00+18:15+19:30 English, `sunday[5]` 08:00 English / 09:30 Tamil / 11:00 English / 12:30 English / 18:30 Tamil, `publicHoliday` 09:00 English · 10:00 Tamil, `confession` 15 min before Mass, `adoration` Rosary/Divine Mercy/Sat Rosary, `note` office closed gates 17:00) + `contact` (office +65 6294 0624, fax +65 6294 2686, `colol.secretariat@catholic.org.sg`) + `transport` (Bugis EW12/DT14 · Rochor DT13 · Jalan Besar DT22, buses Ophir/Victoria: 2,7,12,32,33,51,61,63,80,130,133,145,197) + `feast` Our Lady of Lourdes — 11 February + `uen` "" (no UEN) / `chequePayee` Church of Our Lady of Lourdes / `facebook` ChurchOfOurLadyOfLourdes / `instagram` "" / `youtube` "" / `archdiocese` catholic.sg / `mapsUrl`/`mapsEmbedSrc` 50 Ophir Road — Footer + Worship + About consume it, don't duplicate. Pages render from data — don't inline copy.
- **Components:** `Button` (discriminated `to`/`href`/native `button` + `icon`; variants `primary|secondary|ghost|outline-light`; `active` press feedback), `Container` (`max-w-7xl px-5 sm:px-8`), `SectionHeading` (`eyebrow/title/description` + `align/light` + line), `PageHero` (`compact?`, `bg-grain` + dual gradients + `rise-in` staged content), `Reveal` (`delay`/`as`), `Accordion` (single-open, animated grid-rows collapse, `inert` + `aria-hidden` closed panels, arrow-key navigation), `Timeline` (left rail drawn on scroll + `dot-pulse` halos — fed 1884–2005 Ophir milestones), `SafeImage` (`src` + `fallback` + `alt` + `loading` + optional `fetchPriority`; always via `cn()`), `BackToTop` (threshold 480 + SVG ring + reduced-motion-aware, hash-safe), `ScrollProgress` (decoupled rail), `SocialIcons` (Facebook via custom SVG — Instagram/YouTube icons present in component but empty in site.ts). Ministries pill active state is scrollspy-driven via `useScrollSpy`. Extend via `cn()`, not ad-hoc class strings.
- **Styling:** Use `bsc-cream/parchment(+dark)/stone/ink/charcoal/sapphire-*/gold-*/terracotta-*/pine-*` + `shadow-bsc`/`shadow-bsc-lg` + utilities `text-balance` / `bg-adobe-texture` / `bg-grain` / `bg-gold-bloom` / `divider-weave`/`divider-weave-thin` / `gold-rule`/`gold-rule-left` / `hero-ken-burns` / `rise-in`(+`-d1..d4`) / `menu-in` / `drawer-in` / `drawer-item-in` / `page-in` / `dot-pulse` / `card-lift` / `card-tint` / `link-underline` / `reveal`+`reveal-visible` / `skip-link` / `mask-fade-b` / `img-zoom` / `scrim-hero` / `scrim-page` / `hero-fade` / `rule-draw` + `bloom-drift`. Tokens are 33 colors (incl. `gold-700 #85641c`, `terracotta-600 #8f5038`) + 2 shadows = 35. Motion rule: transform/opacity only, `prefers-reduced-motion` gates everything (global block in `index.css`). Mobile-first (`sm:`/`lg:`). Tokens frame the Neo-Gothic grotto imagery (1888 lancet nave, grotto of St Bernadette, stained glass, parish garden at 50 Ophir Road).

## Don't

- Switch `HashRouter` → `BrowserRouter`, break alias routes, or prop-drill nav arrays. The 7 aliases exist for bookmarks/printed material — keep each `aliasOf` → canonical pair in `App.tsx`.
- Add one-off hex colors or bypass `cn()` (`tailwind-merge` dedup matters).
- Rebuild `Dialog`/`Dropdown` from scratch if `shadcn/ui` (Radix) is adopted — use its primitives.
- Add SSR, API routes, or a CMS without an explicit architecture decision — this is a static SPA (`CLAUDE.md` isolates future CMS behind `lib/cms`).
- Reintroduce Blessed Sacrament / Queenstown content (1 Commonwealth Drive 149603, Sacred Hearts SS.CC since 1958, Tent of Meeting 8 May 1965 folded blue roof Y. Gordon Dowsett, Damien Hall 1963, conserved 2005, Corpus Christi Sunday after Trinity, Queenstown EW19 + Commonwealth EW20, buses 32/51/111/122/145/195/855, phone 6474 0582, UEN T08CC1234A, priests Johan Wongso/William van Soest/Odo Tiggeloven SS.CC) or Risen Christ / Toa Payoh content (91 Toa Payoh Central 319193, first air-con $450k, Grateful/Faithful/Sent, He is risen, 耶稣复活堂, Velankanni, Toa Payoh NS19, buses 88/157/163 B52261, priests Brian D'Souza/Arun Bellarmin/Dexter Chua, ppc Peter Quek/Audrey Rozario/Calvin Swee/Cheryl-Anne Goh, UEN T08CC4042G) or St Mary of the Angels / Bukit Batok content (5 Bukit Batok East Ave 2, Portiuncula, OFM Custody of St Anthony, WOHA 2004 house of light, Garden of Peace) or reassign `src/data/site.ts` parish facts. OLOL facts are: 50 Ophir Road 188690, first Tamil church 1888 (Meneuvrier MEP 1884, cornerstone 1 Aug 1886, blessed 13 May 1888), Neo-Gothic lancet + stained glass, grotto of Our Lady & St Bernadette, War 1942, Parish for All 1974, National Monument No. 52 (14 Jan 2005, restored 2009–2010), feast 11 February, Bugis EW12/DT14 · Rochor DT13 · Jalan Besar DT22, buses Ophir/Victoria 2,7,12,32,33,51,61,63,80,130,133,145,197, phone 6294 0624, no UEN, chequePayee Church of Our Lady of Lourdes, priests Alphonsus Dominic / Leo Justin HGN / Joachim Meneuvrier MEP. Hours, Mass, and address are the single source — don't duplicate them across pages.
- Assume gates are green — run `pnpm lint && pnpm typecheck && pnpm test && pnpm test:e2e && pnpm build` before shipping; lint + typecheck + build are authoritative in this worktree (no unit tests present — `pnpm test` reports 0). Asset-path e2e assertions must be env-agnostic.

## Where to look next

- `CLAUDE.md` — full six-phase workflow, detailed conventions, anti-patterns, env contract, and validation checklist.
- `src/index.css` — authoritative token list (33 colors + 2 shadows; utilities + 9 keyframes; card-tint + scrims present).
- `src/data/site.ts` — canonical parish facts (address, hours, mass, transport, feast, contact, maps). Don't duplicate.
- `src/data/content.ts` — typed content arrays (timeline, grounds, ministries, faqs, events, giving, devotions, visitorGuidelines, images).
- `src/data/nav.ts` — navigation single source (`primaryNav[6]` + `footerNav[10]`).
- `public/_headers` — security headers served on static host (nosniff, DENY, Permissions-Policy, HSTS).
- `scripts/inject-csp-hashes.mjs` — post-build CSP sha256 pinning; see inline JSDoc.
- `blessed-sacrament-queenstown_SKILL.md` — historical lineage stub (BSC hop, Queenstown) — not current canonical; retained for provenance.
- `docs/` — prior round audits and enhancement plans (historical, BSC-era). Treat as lineage, not current spec. Current worktree has no `src.orig/` dir — historical refs to a 77-file archive describe prior port lineage, not a checked-out tree; `git log --all -- src.orig` is empty — don't look for it on disk.
